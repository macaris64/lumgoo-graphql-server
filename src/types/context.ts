/**
 * GraphQL context type definition
 * Used by GraphQL Codegen to generate typed resolver signatures
 */
export interface Context {
  // Add context properties here as needed
  // For example:
  // userId?: string;
  // userRole?: string;
  // dataSources?: DataSources;
}
